main = {
  assert(and; "and;")
  assert(not . and false;  "and false;")
  assert(      and true;   "and true;")
  assert(not . and false false;  "and false false;")
  assert(not . and false true;   "and false true;")
  assert(not . and true  false;  "and true false;")
  assert(      and true  true;   "and true true;")
  assert(not . and true  true  false; "and true true false;")
  assert(      and true  true  true;  "and true true true;")

  assert(not . or; "or;")
  assert(not . or  false;  "or false;")
  assert(      or  true;   "or true;")
  assert(not . or  false false;  "or false false;")
  assert(      or  false true;   "or false true;")
  assert(      or  true  false;  "or true false;")
  assert(      or  true  true;   "or true true;")
  assert(not . or  false false false; "or false false false;")
  assert(      or  false false true;  "or false false true;")
}
